# services/gateway/app/models.py
# Gateway не работает с БД напрямую, поэтому файл пока пустой
# Но он нужен для единообразия структуры

from sqlalchemy.orm import declarative_base

Base = declarative_base()